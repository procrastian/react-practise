function AppTitle (props) {

    return (
        <div className="App">
        <header className="App-header">
            <p>{props.projectTitle}</p>
        </header>
      </div>  
    )

}

export default AppTitle